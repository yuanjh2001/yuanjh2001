<resources>

    <style name="Xf_common_UpdateApkDialog" parent="@android:style/android:Theme.Dialog">
        <!--windowFrame框为无 -->
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 除去背景 -->
        <item name="android:windowBackground">@drawable/xf_common_dialog_bg</item>
        <!--是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 去掉覆盖层 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 设置背景颜色 -->
        <item name="android:background">#00000000</item>
    </style>

    <style name="Xf_common_NetDialog" parent="@android:style/android:Theme.Dialog">

        <!-- windowFrame框为无 -->
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 除去背景 -->
        <item name="android:windowBackground">@drawable/xf_common_dialog_bg</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 去掉覆盖层 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 设置背景颜色 -->
        <item name="android:background">#00000000</item>
    </style>

</resources>